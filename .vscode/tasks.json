{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "gcc build current file",
            "command": "bash",
            "args": [
                "-c",
                "if [ -f \"${fileDirname}/csapp.c\" ]; then gcc -g -O0 -pthread \"${file}\" \"${fileDirname}/csapp.c\" -o \"${fileDirname}/${fileBasenameNoExtension}\"; else gcc -g -O0 -pthread \"${file}\" -o \"${fileDirname}/${fileBasenameNoExtension}\"; fi"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": ["$gcc"],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "compile current file (auto-link csapp.c if present)"
        }
    ]
}
